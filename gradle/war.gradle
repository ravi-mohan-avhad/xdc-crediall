apply plugin: "war"

bootWar {
    mainClass = "com.crediall.api.CrediallApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
